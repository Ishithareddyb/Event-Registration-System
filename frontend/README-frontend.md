# Frontend (React — Pro)

This upgraded frontend uses:
- TailwindCSS via CDN for quick pastel-blue styling
- Chart.js via CDN for dashboard charts
- SweetAlert2 via CDN for modals and toast notifications

Install and run:
1. npm install
2. npm start
Frontend expects backend at http://localhost:8080
